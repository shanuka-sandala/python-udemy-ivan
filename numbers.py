import math

radius = float(input("Enter the radius: "))
area = math.pi * (radius **2)
cir = 2 * math.pi * radius

print ("Area of the circle is ", round(area,2))
print ("Circumference of the cirle is ", round(cir,2))